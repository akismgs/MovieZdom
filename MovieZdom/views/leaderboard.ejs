<!DOCTYPE html>
<html lang="el">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Κατάταξη - MovieZdom</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        body { 
            background-color: #1a1a2e; 
            color: white; 
            padding-top: 100px;
            min-height: 100vh;
            font-family: 'Segoe UI', sans-serif;
        }
        .leaderboard-card {
            background: #16213e;
            border-radius: 20px;
            border: 1px solid #e94560;
            padding: 2.5rem;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
        }
        .leaderboard-table {
            background: transparent;
            color: white;
            border-collapse: separate;
            border-spacing: 0 0.75rem;
        }
        .leaderboard-table thead {
            border-bottom: none;
        }
        .leaderboard-table thead th {
            border: none;
            color: #e94560;
            font-weight: bold;
            text-transform: uppercase;
            font-size: 0.85rem;
            letter-spacing: 1.5px;
            padding: 1rem 1.5rem;
            background: transparent;
        }
        .leaderboard-table tbody tr {
            background: #0f3460;
            border-radius: 15px;
            border: 1px solid rgba(255,255,255,0.05);
            transition: all 0.3s ease;
            margin-bottom: 0.5rem;
        }
        .leaderboard-table tbody tr:hover {
            transform: translateY(-3px);
            border-color: #e94560;
            box-shadow: 0 4px 15px rgba(233, 69, 96, 0.3);
        }
        .leaderboard-table tbody td {
            border: none;
            padding: 1.25rem 1.5rem;
            vertical-align: middle;
            background: transparent;
        }
        .leaderboard-table tbody td:first-child {
            border-top-left-radius: 15px;
            border-bottom-left-radius: 15px;
        }
        .leaderboard-table tbody td:last-child {
            border-top-right-radius: 15px;
            border-bottom-right-radius: 15px;
        }
        .rank-badge {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 50px;
            height: 50px;
            border-radius: 50%;
            font-weight: bold;
            font-size: 1.2rem;
            box-shadow: 0 4px 10px rgba(0,0,0,0.3);
            transition: transform 0.3s ease;
        }
        .rank-badge:hover {
            transform: scale(1.1);
        }
        .rank-1 { 
            background: linear-gradient(135deg, #ffd700, #ffed4e); 
            color: #1a1a2e; 
            box-shadow: 0 4px 15px rgba(255, 215, 0, 0.4);
        }
        .rank-2 { 
            background: linear-gradient(135deg, #c0c0c0, #e8e8e8); 
            color: #1a1a2e; 
            box-shadow: 0 4px 15px rgba(192, 192, 192, 0.4);
        }
        .rank-3 { 
            background: linear-gradient(135deg, #cd7f32, #daa520); 
            color: white; 
            box-shadow: 0 4px 15px rgba(205, 127, 50, 0.4);
        }
        .rank-other { 
            background: #16213e; 
            color: #e94560; 
            border: 2px solid #e94560;
        }
        .username-text {
            font-weight: 600;
            font-size: 1.15rem;
            color: white;
        }
        .wins-text {
            color: #e94560;
            font-weight: bold;
            font-size: 1.3rem;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }
        .wins-text i {
            font-size: 1.2rem;
        }
        .navbar { 
            background-color: #0f3460 !important; 
            z-index: 1030;
        }
        .trophy-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
            background: linear-gradient(135deg, #ffd700, #ffed4e);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            filter: drop-shadow(0 0 10px rgba(255, 215, 0, 0.5));
        }
        .empty-message {
            text-align: center;
            padding: 4rem 2rem;
            color: #6c757d;
        }
        .header-section {
            margin-bottom: 2rem;
        }
        .header-section hr {
            margin-top: 1.5rem;
            margin-bottom: 0;
        }
        ::-webkit-scrollbar { width: 8px; }
        ::-webkit-scrollbar-track { background: #1a1a2e; }
        ::-webkit-scrollbar-thumb { background: #e94560; border-radius: 10px; }
    </style>
</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-dark shadow-sm fixed-top">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/dashboard">MovieZdom 🎮</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item"><a class="nav-link" href="/dashboard">Παιχνίδια</a></li>
                    <li class="nav-item"><a class="nav-link" href="/create-game">Δημιουργία</a></li>
                    <li class="nav-item"><a class="nav-link active" href="/leaderboard">Κατάταξη</a></li>
                </ul>
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a href="/dashboard" class="btn btn-outline-light btn-sm px-3 shadow-sm">Επιστροφή</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-10 col-lg-8">
                <div class="leaderboard-card">
                    <div class="text-center header-section">
                        <i class="bi bi-trophy-fill trophy-icon"></i>
                        <h2 class="fw-bold mb-2">Πίνακας Κατάταξης</h2>
                        <p class="text-secondary mb-0">Οι 10 καλύτεροι παίκτες με τις περισσότερες νίκες</p>
                        <hr class="border-secondary opacity-25">
                    </div>

                    <% if (topUsers && topUsers.length > 0) { %>
                        <div class="table-responsive">
                            <table class="table leaderboard-table">
                                <thead>
                                    <tr>
                                        <th style="width: 100px;" class="ps-4">#</th>
                                        <th>Όνομα Χρήστη</th>
                                        <th class="text-end pe-4">Νίκες</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% topUsers.forEach((user, index) => { %>
                                        <tr>
                                            <td class="ps-4">
                                                <% const rank = index + 1; %>
                                                <% if (rank === 1) { %>
                                                    <span class="rank-badge rank-1">🥇</span>
                                                <% } else if (rank === 2) { %>
                                                    <span class="rank-badge rank-2">🥈</span>
                                                <% } else if (rank === 3) { %>
                                                    <span class="rank-badge rank-3">🥉</span>
                                                <% } else { %>
                                                    <span class="rank-badge rank-other"><%= rank %></span>
                                                <% } %>
                                            </td>
                                            <td>
                                                <span class="username-text"><%= user.username %></span>
                                            </td>
                                            <td class="text-end pe-4">
                                                <span class="wins-text">
                                                    <i class="bi bi-trophy-fill"></i>
                                                    <%= user.stats.wins || 0 %>
                                                </span>
                                            </td>
                                        </tr>
                                    <% }) %>
                                </tbody>
                            </table>
                        </div>
                    <% } else { %>
                        <div class="empty-message">
                            <i class="bi bi-trophy display-1 text-secondary opacity-25"></i>
                            <h4 class="text-secondary mt-3">Δεν υπάρχουν ακόμα παίκτες στον πίνακα κατάταξης.</h4>
                            <p class="text-secondary mt-2">Παίξτε παιχνίδια για να εμφανιστείτε στον πίνακα!</p>
                        </div>
                    <% } %>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

